const {usuarios,data_persona,tipo_usuario,usuarios_verificacion} = require("../db");
const { Op } = require("sequelize");
const crypto = require("crypto");
const jwt = require("jsonwebtoken");
const secretKey = process.env.SECRETKEY;
const Sequelize = require("sequelize");

exports.create= (data)=>{
    let result={};
    try {
        
    } catch (error) {
        
    }
    return result;
}
exports.update= (data)=>{
    let result={};
    try {
        
    } catch (error) {
        
    }
    return result;
}
exports.findAll= ()=>{
    let result={};
    try {
        
    } catch (error) {
        
    }
    return result;
}
exports.FindOne= (id)=>{
    let result={};
    try {
        
    } catch (error) {
        
    }
    return result;
}